import { Breadcrumb } from "sympathetic-ds";

/** Field-notes article breadcrumb — used at the top of every observation. */
export function Default() {
  return <Breadcrumb observationNumber="042" />;
}
